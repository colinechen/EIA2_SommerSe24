"use strict";
/*namespace classes {
    export class Bush {
        x: number;
        y: number;
        color: string;
     

        constructor (_x:number, _y:number){
            console.log("Bush Constructor")
            this.x = _x;
            this.y = _y;
        }


    draw(): void {
        crc2.save();
        crc2.translate(this.x, this.y);
    
        // Busch zeichnen
        crc2.beginPath();
        crc2.arc(30, 20, 10, 0, Math.PI * 2); // Kreis 1
        crc2.arc(45, 15, 12, 0, Math.PI * 2); // Kreis 2
        crc2.arc(60, 20, 10, 0, Math.PI * 2); // Kreis 3
        crc2.arc(40, 25, 10, 0, Math.PI * 2); // Kreis 4
        crc2.arc(50, 25, 10, 0, Math.PI * 2); // Kreis 5
        crc2.arc(55, 18, 8, 0, Math.PI * 2); // Kreis 6
        crc2.arc(35, 18, 8, 0, Math.PI * 2); // Kreis 7
        crc2.arc(50, 20, 10, 0, Math.PI * 2); // Kreis 8
        crc2.arc(40, 15, 10, 0, Math.PI * 2); // Kreis 9
        crc2.arc(55, 23, 10, 0, Math.PI * 2); // Kreis 10
        crc2.arc(45, 25, 8, 0, Math.PI * 2); // Kreis 11
    
        let bushColor: string = "darkgreen";
        crc2.fillStyle = bushColor;
        crc2.fill();
    
        crc2.restore();
    }
}

} */ 
//# sourceMappingURL=bush.js.map